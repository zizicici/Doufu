# 执行计划

## 当前状态（2026-03-05）

1. 已完成首页项目画廊（搜索、长按菜单、拖拽排序、新建入口）。
2. 已完成项目运行页（全屏预览 + 悬浮面板 + 退出确认）。
3. 已完成项目级设置页（名称修改）。
4. 已完成 Provider 管理全链路（API Key + OAuth）。
5. 已完成 Codex 聊天改文件链路（流式 + 补丁落盘 + 调试日志）。
6. 已完成超时与推理分级策略（`high=400s`，`xhigh=600s`）。

## 已完成阶段回顾

1. Phase A：基础框架
   - 文档骨架、目录规范、核心模块边界。
2. Phase B：项目生命周期
   - 本地项目创建、读取、删除、改名、更新时间维护。
3. Phase C：首页交互
   - 卡片网格展示、搜索、空状态、上下文菜单、排序页。
4. Phase D：运行与编辑入口
   - `WKWebView` 运行页、悬浮面板、刷新与退出流程。
5. Phase E：Provider 与认证
   - Manage Providers、Add Provider、API Key 与 OAuth 两种流程。
6. Phase F：Codex 聊天改工程
   - 文件上下文、流式请求、JSON 补丁解析、安全写入、日志排障。

## 下一阶段计划

1. Phase G：聊天体验增强
   - 增量更新稳定性、失败重试策略可视化、历史版本回滚。
2. Phase H：项目调试能力
   - 运行日志可视化、构建前检查、错误定位辅助。
3. Phase I：Provider 生态扩展
   - 增加 Provider 类型、连通性检测、配置导入导出。
4. Phase J：分享与导出
   - 项目导出包、隐私检查、分享入口。

## 验收口径（下一阶段）

1. 聊天失败场景可定位原因并有明确恢复路径。
2. 关键改动可回滚，不破坏项目目录完整性。
3. 新增能力不破坏现有项目运行与 Provider 配置。
